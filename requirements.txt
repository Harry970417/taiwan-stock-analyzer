# requirements.txt — Taiwan Stock Factor Analyzer (Phase 1)
# Updated: 2026-06-19 | Platform: Windows 11 | Python 3.14.5
#
# NOTE: Python 3.14 is a pre-release. For production reproducibility
# use Python 3.11.x or 3.12.x (stable LTS). All versions below are
# confirmed working on 3.14.5 as of the study date.
#
# Install: pip install -r requirements.txt
# Reproduce exactly: pip install --no-deps -r requirements.txt

# --- Core scientific stack (Phase 1 confirmed versions) ---
pandas>=2.0.0,<3.0
numpy>=1.24.0,<2.0
scipy==1.17.1

# --- Financial data ---
yfinance==1.4.1
requests==2.34.2

# --- Visualisation ---
plotly==6.8.0

# --- Utilities ---
python-dateutil==2.9.0.post0
pytz==2026.2

# --- Application (Phase 0 dashboard, not required for Phase 1 pipeline) ---
streamlit==1.58.0
matplotlib==3.10.9
sqlalchemy==2.0.50
ta==0.11.0
scikit-learn>=1.3.0
openpyxl>=3.1.0
