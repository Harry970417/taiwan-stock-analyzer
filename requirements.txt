# requirements.txt — Taiwan Stock Factor Analyzer (Phase 1)
# Updated: 2026-08-03 | Platform: Windows 11 | Python 3.11 (pinned production runtime)
#
# SUPPORTED runtime: Python 3.11.x (matches .python-version, runtime.txt,
# Dockerfile). Verified 2026-08-03 in a brand-new virtual environment:
# `pip install -r requirements.txt` installs cleanly (pandas 2.3.3, numpy
# 1.26.4), `pytest tests/` passes all 252 tests, `streamlit run app.py`
# serves correctly. Python 3.12 also verified clean as a fallback.
#
# UNSUPPORTED: Python 3.14. numpy<2.0 has no prebuilt wheel for 3.14 and
# will attempt a from-source build requiring a C/C++ toolchain (meson +
# a compiler) that is not present on this machine, causing `pip install`
# to fail outright. The environment that produced results/metadata.json
# (Python 3.14.5, pandas 3.0.3, numpy 2.4.6) was a local interactive-dev
# environment with those packages already present system-wide, NOT a
# `pip install -r requirements.txt` from this file on that Python version.
# Do not deploy on Python 3.14. See docs/DEPLOYMENT_READINESS_PHASE4.md
# and reproducibility_manifest.md item #3 for the full history.
#
# Install: pip install -r requirements.txt
# Reproduce exactly: pip install --no-deps -r requirements.txt

# --- Core scientific stack (Phase 1 confirmed versions) ---
pandas>=2.0.0,<3.0
numpy>=1.24.0,<2.0
scipy==1.17.1

# --- Financial data ---
yfinance==1.4.1
requests==2.34.2

# --- Visualisation ---
plotly==6.8.0

# --- Utilities ---
python-dateutil==2.9.0.post0
pytz==2026.2

# --- Application (Phase 0 dashboard, not required for Phase 1 pipeline) ---
streamlit==1.58.0
matplotlib==3.10.9
sqlalchemy==2.0.50
ta==0.11.0
scikit-learn>=1.3.0
openpyxl>=3.1.0
