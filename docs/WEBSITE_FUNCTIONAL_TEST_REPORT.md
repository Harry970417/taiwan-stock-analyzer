# 網站功能測試報告

測試日期：2026-07-31
方式：`pytest` 全量測試 + 本機啟動 `streamlit run app.py` + Playwright 瀏覽器實際操作 + 截圖

---

## 1. 單元測試

```
python -m pytest tests/ -q
→ 163 passed（原 160 + 本次新增 3）
```

README/PROJECT_AUDIT 聲稱的「160 通過」與實際執行結果一致，未發現測試數字造假。

## 2. App 啟動 Smoke Test

`streamlit run app.py` 可正常啟動，首頁與側邊欄 14 個頁面（app / 市場動能分析 / 走勢預測分析 /
即時市場分析 / 短線機會掃描 / 個股量化分析 / 投資組合管理 / 因子選股 / 策略驗證中心 /
法人籌碼分析 / Fundamental Factors TW / 數據驗證中心 / 多因子回測中心 / 投資組合風險引擎 /
研究報告產生器）皆可透過導覽列開啟，無 404 或崩潰。

過程中發現一個**環境問題（非程式邏輯 bug）**：測試期間曾有舊的 `streamlit` 背景行程未確實關閉，
導致新程式碼修改後瀏覽器仍看到舊行程回應（同一 port 被舊行程占用）。已確認排除後重新驗證，
記錄於此供後續除錯參考：若本機測試發現「改了程式碼但畫面沒變」，先確認 `ps aux | grep streamlit`
是否有多個行程。

## 3. 首頁瀏覽器實測

- 首頁載入正常，可見研究平台定位、資料來源標籤（Yahoo Finance / TWSE 官方資料 / FinMind API）、
  T+1 無未來函數與 Walk-Forward 驗證標示。
- 免責聲明明確：「本平台所有分析結果僅供學術研究與作品集展示，不構成任何投資建議。」
- **發現問題**：首頁原本只顯示「頁面渲染時間」，沒有顯示市場資料實際所屬交易日。
  實測當下 TWSE 官方 API 回傳的最新資料是 **2026-07-30**（非當日 2026-07-31），
  若不明確標示，使用者容易誤以為畫面是即時資料。**已修復**（見 `UI_UX_REDESIGN_REPORT.md`）。
- 市場情緒 KPI（上漲/下跌家數、市場均漲跌幅、A/D Ratio）、今日強勢股 Top5、成交量 Top5、
  投資分析決策流程（7 步驟）、平台研究能力區塊皆正常渲染，無元件重疊或文字截斷。

## 4. 個股量化分析頁面實測

- 空狀態正確：進入頁面未輸入資料前顯示「👈 輸入股票代號，按下『開始深度分析』」引導文字。
- 輸入 `2330` 並點擊「開始深度分析」後，實際取得 Yahoo Finance 即時報價
  （$242.50，延遲約 15 分鐘，更新時間標示清楚），並渲染四維評級（動能／估值／成長／財務）卡片。
- 確認資料來源與延遲時間有明確揭露，未偽裝成即時報價。

## 5. 因子選股頁面實測

- 空狀態正確：顯示「👈 設定條件後按下『開始篩選』」與預設產業清單（半導體／IC設計／伺服器AI／
  電子製造／金融銀行／電信網路／生技醫療／觀光旅遊／大型指數ETF／高息ETF）供快速選取。

## 6. Console 錯誤檢查

瀏覽器 console 出現的錯誤僅為 Streamlit 框架自身的 `/_stcore/health`、`/_stcore/host-config`
在子頁面路徑下的 404（框架已知行為，與本專案程式碼無關），未發現本專案程式碼拋出的 JS 錯誤。

## 7. 尚未逐頁測試的頁面

受限於時間，本輪未逐一點擊測試以下頁面的完整互動流程（僅確認可開啟、無崩潰）：
走勢預測分析、即時市場分析、短線機會掃描、投資組合管理、策略驗證中心、法人籌碼分析、
Fundamental Factors TW、數據驗證中心、多因子回測中心、投資組合風險引擎、研究報告產生器。

建議下一輪：針對「法人籌碼分析」「多因子回測中心」實際輸入資料跑一次完整流程並截圖存證，
因為這兩頁涉及外部 API（三大法人買賣超、多因子回測）較容易受資料源異動影響。

## 8. 結論

- 核心功能（測試、啟動、首頁、個股分析、因子選股）驗證通過，README 對測試數量與功能的
  描述與實測一致，未發現造假或誇大。
- 修復 1 項真實 UX 缺口（資料日期不透明）。
- 專案已有完整的 `PROJECT_AUDIT_2026.md` / `PROJECT_REMEDIATION_PLAN.md` / `PROJECT_REMEDIATION_LOG.md`
  三份文件記錄先前多輪整改，Tier S 待辦清單仍有效，未在本輪重複或推翻。
